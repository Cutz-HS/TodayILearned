[python-analysis]▶
# 파이썬 스파게티 코딩을 막기 위한 방식 ▶ #클래스선언 #함수선언 #변수선언 #메인코드

# 전역 / 지역변수
 ▶ 가까운 변수를 우선한다.
 ▶ 함수 안에서 전역변수를 사용하기 위해서는 global 호출을 해줘야 한다.

# 변수 이름은 _로 분류 // 단어 후 대문자(앞글자)

# int 다중 선언 ▶ [int] * n

# event: 행위, 기능 -> event-driven programming -> event 발생해야 동작이 시작
 -> db보다 lisenter가 죽었을 때, 오류가 주로 발생
 -> 매개변수에 함수 -> 콜백함수

# 논리적 곱
 ▶ 마지막 1 확인 -> 비트연산(마지막 체크)
 '>>=' -> 비트쉬프트 (비트를 1칸씩 이동)

# for문 모두 나오기
 ▶ switch를 이용해서 나가기
  -> sw = False -> sw = True로 변환

# 대문자변수는 상수 취급

# "%2d" -> 오른쪽 정렬