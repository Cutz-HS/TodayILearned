[python-analysis]▶
1. 딥러닝 RNN study
  - 기계학습 알고리즘 -> 뉴럴네트워크 -> 딥러닝, 역전도화

2. 정보관리시기술사&정보시스템감리사(KCISA)
  - 응시자격이 기사자격증&경력&학위 -> 노년 안정적

3. 레벤슈타인거리
 ▶ 두개의 문자열이 어느정도 다른지 편집거리를 통해 알아보는 알고리즘
 - 편집은 추가/변경/삭제
 - 첫 행과 첫 열은 0부터 n까지 증가
 - 요소별 비교를 통해 같다면, 비교요소의 (n-1,n-1)의 요소 대입(n-1/n-1까지의 요소비교)
 - 다르다면, 좌(추가)/위(삭제)/대각선(변경) 중 min값+1
 - return은 끝행의 끝열

4. 최장공통부분수열알고리즘(lcs)
 ▶ 레벤슈타인거리와 원리는 같지만, 유사거리를 리턴

5. numpy 0행렬
 ▶ numpy.zeros((행,열))

6. N-gram
 ▶ 두 문장 사이의 유사도를 조사하는 방법
 ▶ n은 띄어쓰기를 포함한 문장을 쪼개는 단어수
 - 두 문장을 n으로 쪼개서 리스트화
 - 리스트의 요소끼리 비교하여 공통요소가 있을 경우, count+1
 - 리스트의 요소에 count를 나눠주는 값을 리턴

7. 선형변환
 ▶ 행렬곱 후에 벡터의 크기와 방향이 변환되도록 하는 것
 ▶ 변환되도록 하는 행렬을 "가중치"라고 하며, 딥러닝에서는 이 가중치를 업데이트하며 학습시킨다.
   - 행렬곱 후에 원하는 값이 나오도록 가중치를 sim

8. 특징선택
 ▶ 입력데이터의 차원을 줄이는 issue를 위해 변수를 임의로 줄여내는 것
  - 좌표상의 데이터를 하나의 축으로 전환시키는 것

9. 차원축소
  - m차원벡터를 출력하는 선형수를 만들어내 손실을 최소화한다.
  - 고유벡터값을 이용한다.

10. 고유벡터
 ▶ 행렬곱을 하여 상수곱행렬값을 갖게하는 x벡터를 고유벡터라 칭한다.
 ▶ 해당 상수람다는 A행렬의 고유값